export interface FormData {
  name: string;
  icon: string;
}

export interface PosterRes {
  title: string;
  content: string;
  city: string;
  category: string;
  amount: string;
}

export type paramsId = { id: string };
